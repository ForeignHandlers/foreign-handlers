export const tab = '  ';
export const emptyLine = '\n\n';

export const fileImport = "import { HandlersData } from '$core/types';";

export const handlersType = {
  start: 'export type Handlers = {',
  end: '};',
};

export const handlersData = {
  start: 'export const handlersData: Record<string, HandlersData> = {',
  end: '};',
};
